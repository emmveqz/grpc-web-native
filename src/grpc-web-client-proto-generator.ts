
/**
 * @ToDo
 * Create a code generator that takes the already generated code
 * from the `protobufjs` tools `pbjs` and `pbts` to read the definitions and
 * create a resulting code such as what we see in /example/generated/grpc-web-client-protos.ts
 */
